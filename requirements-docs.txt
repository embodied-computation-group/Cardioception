# The package itself is installed separately with --no-deps: the API pages only
# need the docstrings, and PsychoPy's GUI stack does not build on the runner.
# cardioception.reports and the parameters modules still import these at module
# level, so autodoc needs them present (the rest are mocked in conf.py).
numpy
pandas
matplotlib

# Documentation dependencies (minimal set to avoid conflicts)
sphinx==5.3.0
sphinxcontrib-bibtex>=2.4.2
numpydoc>=1.4.0
myst-parser
myst-nb>=0.16.0
sphinx-design==0.5.0

# Theme. Pinned because furo 2023.5+ requires Sphinx 6, and Sphinx is held at 5.3.
furo==2023.3.27

# Canonical URLs, sitemap and per-page metadata. Both are pinned to the last
# release that still supports Sphinx 5: sphinxext-opengraph 0.10 requires
# Sphinx 6, and sphinx-sitemap 2.7 adds a sphinx-last-updated-by-git dependency
# this build does not need. opengraph draws its preview cards with Matplotlib,
# which is already installed above.
sphinx-sitemap==2.6.0
sphinxext-opengraph==0.9.1

# Visualization (compatible versions)
graphviz

# Note: Removed problematic packages that cause dependency conflicts
# - xarray (conflicts with numpy version)
# - bokeh (conflicts with systole requirements)
# - pymc, metadpy, watermark (optional packages)
