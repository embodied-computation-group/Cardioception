numpy>=1.21
scipy>=1.3.0
pandas>=1.0.3
# Tested against 2026.2.2, which is what CI installs. The floor is 2026.2
# because that is the oldest release verified against this code; the ceiling is
# a major version, because 2026 moved visual.RatingScale into a plugin without
# a deprecation cycle and the next one may do the same. The weekly install
# workflow catches drift inside the range before a student does.
psychopy>=2026.2,<2027
pyserial>=3.4
matplotlib>=3.3.3
seaborn>=0.11.1
pingouin>=0.3.10
papermill>=2.3.1
systole-core>=0.3.0
# No setuptools ceiling: psychopy 2026.2.2 does not import pkg_resources.
# Verified by checking sys.modules after importing visual, data, gui and sound.
setuptools>=38.4
packaging